import { fallbackLng } from '@src/app/i18n/settings'
import { IProjectsPageProps } from './ProjectsPage'

const base: IProjectsPageProps = {
  lang: fallbackLng
}

export const mockProps = {
  base
}
